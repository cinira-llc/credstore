/* Note: This file is seen by both the build process and by the IDE. Most configuration should be done here. The
complete config for the build process is in tsconfig-build.json, which extends this file and adds some items that cause
the IDE to misbehave in various ways. */
{
  "compilerOptions": {
    "strict": true,
    "target": "ESNext",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "esModuleInterop": true,
    "lib": [
      "ESNext"
    ],
    "outDir": "dist"
  },
  "exclude": [
    "dist",
    "node_modules"
  ]
}